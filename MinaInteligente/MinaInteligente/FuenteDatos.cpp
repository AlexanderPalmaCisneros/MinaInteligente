#include "FuenteDatos.h"

using namespace MinaInteligente;

// FuenteDatos es abstracta y su unico metodo (ObtenerLectura) es virtual puro:
// no tiene cuerpo propio, lo definen sus clases derivadas (FuenteDatosReal, FuenteDatosSimulada).
